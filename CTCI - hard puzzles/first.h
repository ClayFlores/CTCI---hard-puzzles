#pragma once
#include <iostream>
#include <bitset> // to convert to binary
#include <string>

using std::cout;
using std::endl;
using std::string;

// 1.) Write a function that adds two numbers. Don't use + or arithmetic operators
 void addTwo(int, int);